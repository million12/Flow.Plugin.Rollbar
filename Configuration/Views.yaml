-
  requestFilter: 'isPackage("Neos.Neos") && isFormat("html") && isController("Backend\Module")'
  options:

    # Override layout paths, so we can we can override Neos Default.html layout
    layoutRootPaths:
      'M12.Rollbar': 'resource://M12.Rollbar/Private/Layouts'
      'Neos.Neos': 'resource://Neos.Neos/Private/Layouts'
